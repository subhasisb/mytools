---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: pbsjobs.batch.example.com
spec:
  group: batch.example.com
  names:
    kind: PBSJob
    listKind: PBSJobList
    plural: pbsjobs
    singular: pbsjob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PBSJob is the Schema for the pbsjobs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PBSJob
            properties:
              environment:
                additionalProperties:
                  type: string
                description: Environment specifies environment variables for the job
                type: object
              errorPath:
                description: ErrorPath specifies where to write job error output
                type: string
              jobName:
                description: JobName is the name of the PBS job (used with qsub -N)
                type: string
              outputPath:
                description: OutputPath specifies where to write job output
                type: string
              queue:
                description: Queue specifies which PBS queue to submit the job to
                type: string
              resources:
                additionalProperties:
                  type: string
                description: Resources specifies PBS resource requirements (nodes,
                  ppn, walltime, etc.)
                type: object
              script:
                description: Script is the PBS job script content to be executed
                type: string
              walltime:
                description: Walltime specifies the maximum execution time for the
                  job
                type: string
              workingDirectory:
                description: WorkingDirectory specifies where the job should execute
                type: string
            required:
            - script
            type: object
          status:
            description: status defines the observed state of PBSJob
            properties:
              completionTime:
                description: CompletionTime is when the job completed
                format: date-time
                type: string
              conditions:
                description: conditions represent the current state of the PBSJob
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              exitCode:
                description: ExitCode is the exit code of the completed job
                format: int32
                type: integer
              message:
                description: Message provides additional information about the current
                  state
                type: string
              pbsJobID:
                description: PBSJobID is the ID assigned by PBS when the job is submitted
                type: string
              phase:
                description: |-
                  Phase represents the current phase of the PBS job
                  Possible values: Pending, Queued, Running, Completed, Failed, Deleted
                type: string
              startTime:
                description: StartTime is when the job started running
                format: date-time
                type: string
              state:
                description: State represents the detailed PBS job state from qstat
                type: string
              submissionTime:
                description: SubmissionTime is when the job was submitted to PBS
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
